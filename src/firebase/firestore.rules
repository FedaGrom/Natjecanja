// Firestore security rules examples
// Copy the desired block into Firebase Console -> Firestore -> Rules

// -------------------------
// DEVELOPMENT (temporary)
// Allows all reads and writes. ONLY use this while actively developing locally.
// Do NOT leave these rules enabled in production.
// -------------------------
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if true;
    }
  }
}

// -------------------------
// PRODUCTION (recommended example using Firebase Authentication)
// Uncomment and adjust to require authenticated users for writes.
// -------------------------
//// rules_version = '2';
//// service cloud.firestore {
////   match /databases/{database}/documents {
////     // Allow anyone to read natjecanja, but only authenticated users may create/update/delete
////     match /natjecanja/{docId} {
////       allow read: if true;
////       allow create, update, delete: if request.auth != null;
////     }
////     // Deny other writes by default
////     match /{document=**} {
////       allow read: if true;
////       allow write: if false;
////     }
////   }
//// }

// -------------------------
// Notes:
// - After editing rules in the console, try the create action again.
// - For authenticated rules, add Firebase Auth to your app and sign in users before write operations.
// - Keep production rules as restrictive as possible.
